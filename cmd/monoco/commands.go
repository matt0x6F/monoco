package main

import (
	"fmt"
	"os"
)

// cmdPropagate is a stub; Task 10 of the v1 plan implements this with
// `plan` and `apply` subcommands.
func cmdPropagate(root string, args []string) {
	fmt.Fprintln(os.Stderr, "monoco propagate: not yet implemented (pending Task 10)")
	os.Exit(1)
}
